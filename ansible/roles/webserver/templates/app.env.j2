# App environment config — generated by Ansible
# This file is encrypted in vault.yml, never appears in logs or git

DB_PASSWORD={{ vault_db_password }}
APP_SECRET={{ vault_app_secret_key }}
MONITORING_TOKEN={{ vault_monitoring_token }}
ENVIRONMENT={{ ansible_ec2_tags.Environment | default('dev') }}